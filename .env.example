# =============================================================
# 复制此文件为 .env.dev 或 .env.prod，并填入实际值
# =============================================================

# -------------------------------------------------------------
# Database (PostgreSQL)
# -------------------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=offer_mate
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456

# -------------------------------------------------------------
# JPA
# -------------------------------------------------------------
# 首次启动用 create，表创建后改为 update；生产环境用 validate 或 none
SPRING_JPA_DDL_AUTO=create

# -------------------------------------------------------------
# Redis
# -------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379

# -------------------------------------------------------------
# AI - 阿里云百炼 DashScope
# -------------------------------------------------------------
AI_BAILIAN_API_KEY=your_api_key_here
AI_MODEL=qwen-plus

# -------------------------------------------------------------
# AI - Vector Store (PgVector)
# -------------------------------------------------------------
AI_VECTORSTORE_INITIALIZE_SCHEMA=true

# -------------------------------------------------------------
# AI - 结构化输出重试
# -------------------------------------------------------------
APP_AI_STRUCTURED_MAX_ATTEMPTS=2
APP_AI_STRUCTURED_INCLUDE_LAST_ERROR=true

# -------------------------------------------------------------
# AI - RAG 配置
# -------------------------------------------------------------
APP_AI_RAG_REWRITE_ENABLED=true
APP_AI_RAG_SHORT_QUERY_LENGTH=4
APP_AI_RAG_TOPK_SHORT=20
APP_AI_RAG_TOPK_MEDIUM=12
APP_AI_RAG_TOPK_LONG=8
APP_AI_RAG_MIN_SCORE_SHORT=0.18
APP_AI_RAG_MIN_SCORE_DEFAULT=0.28

# -------------------------------------------------------------
# Interview
# -------------------------------------------------------------
APP_INTERVIEW_FOLLOW_UP_COUNT=1
APP_INTERVIEW_EVALUATION_BATCH_SIZE=8

# -------------------------------------------------------------
# Storage (RustFS / S3 兼容)
# -------------------------------------------------------------
APP_STORAGE_ENDPOINT=http://localhost:9000
APP_STORAGE_ACCESS_KEY=your_access_key_here
APP_STORAGE_SECRET_KEY=your_secret_key_here
APP_STORAGE_BUCKET=offer-mate
APP_STORAGE_REGION=us-east-1

# -------------------------------------------------------------
# CORS
# -------------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:80

# -------------------------------------------------------------
# JWT
# -------------------------------------------------------------
JWT_SECRET=your-256-bit-secret-key-change-in-production-must-be-at-least-32-characters-long-for-hs256
JWT_EXPIRATION=7200000
JWT_REFRESH_EXPIRATION=604800000